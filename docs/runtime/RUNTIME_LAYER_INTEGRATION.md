# Runtime Layer Integration

Runtime 属于 Layer 5：读取 Layer 1 Memory，服从 Layer 2 决策，执行 Layer 3 工程合同，遵守 Layer 4 Lifecycle / Gate；不覆盖任一 Layer 权威。Intent Gateway 输入分类，Execution Routing 输入 Execution Plan，Runtime 只协调已授权任务。
